---
/**
 * @fileoverview Changelog — what has SHIPPED to production.
 *
 * Renders the two list blocks (changelog24 highlights + changelog3 feed) through
 * the shared view, so this page and its /preview twin can never drift.
 * The proposed / not-yet-deployed twin lives at /admin/changelog/preview.
 */
import BaseLayout from "@/layouts/BaseLayout.astro";
import ChangelogListView from "@/components/changelog/ChangelogListView.astro";
---

<BaseLayout
  title="Changelog - 126 Colby - Remodel Mission Control"
  description="Every change shipped to production, newest first."
>
  <ChangelogListView stage="shipped" />
</BaseLayout>
