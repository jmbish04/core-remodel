/**
 * @fileoverview FaviconService barrel export.
 *
 * Re-exports the `FaviconService` class and the `faviconService` singleton so
 * consumers only need a single import path:
 *
 *   import { faviconService } from "@backend/services/favicon";
 */
export { FaviconService, faviconService } from "./service";
