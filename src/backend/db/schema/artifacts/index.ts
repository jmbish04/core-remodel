/**
 * @fileoverview Artifact Studio schema barrel (0016).
 *
 * The two tables that capture chat-built mini-apps onto the Worker: the
 * `artifacts` metadata head and its immutable `artifact_revisions` chain.
 */
export * from "./artifacts";
export * from "./artifact_revisions";
