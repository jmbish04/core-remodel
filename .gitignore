# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# proofs
proofs/

# python
__pycache__/
*.py[cod]
.venv/

# Local D1 / miniflare state (created by migrate:local / wrangler dev)
.wrangler/
.swarm-context.md
scripts/sketchup/supex

# Local product-intake scrape output (page markdown, downloaded images,
# manifests). Run artifacts, not source — regenerated by
# scripts/product_intake/scrape_product.py.
artifacts/

# Local runtime artifacts from an MCP/proxmox helper dumping into cwd — a DB +
# WAL/SHM sidecars and a log. Never source; regenerated on run.
proxmox-jobs.sqlite3
proxmox-jobs.sqlite3-shm
proxmox-jobs.sqlite3-wal
proxmox_mcp.log
