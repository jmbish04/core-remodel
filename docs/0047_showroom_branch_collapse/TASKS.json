{
  "slug": "showroom-branch-collapse",
  "title": "Collapse chain branches into one business (Tier 2)",
  "tasks": [
    {
      "taskKey": "P1-schema",
      "phase": "P1 — Schema",
      "workstream": "database",
      "changeType": "migration",
      "title": "showroom_merge_candidates + showroom_merge_candidate_members",
      "detail": "group_key UNIQUE (sorted member store ids). Members relate by store_id FK — no denormalized names. role = KEEPER|BRANCH|EXCLUDED; resulting_location_id set on apply. db:generate then migrate:remote, then verify on remote.",
      "dependsOn": [],
      "status": "pending"
    },
    {
      "taskKey": "P1-unit-column",
      "phase": "P1 — Schema",
      "workstream": "database",
      "changeType": "migration",
      "title": "Add unit/suite column to showroom_store_locations",
      "detail": "The locations table cannot express a suite, so any address rebuilt from street_number + street_name is BUILDING-level — that is what nearly merged Leandro Quintal (#64A) into Marblus Granite (#40C) in PR #356, and why locations were reduced to contributing place_id only. After 0047 most branch addresses live ONLY on location rows, so they must be unit-qualifiable before address matching can safely use them again. Add the column, backfill from the parsed store location_address where possible, then re-enable the location address signal in dedup with the unit included.",
      "dependsOn": [
        "P1-schema"
      ],
      "status": "pending"
    },
    {
      "taskKey": "P2-scan",
      "phase": "P2 — Scan",
      "workstream": "backend",
      "changeType": "added",
      "title": "scan_showroom_merge_candidates — upsert candidates from 0046 detection",
      "detail": "Reuses groupBySignals + the isReal>=2 classification; does NOT reimplement detection. Idempotent by group_key. A group whose membership changed becomes a NEW candidate and the old goes STALE, so a pending decision is never mutated underneath.",
      "dependsOn": [
        "P1-schema"
      ],
      "status": "pending"
    },
    {
      "taskKey": "P3-collapse-service",
      "phase": "P3 — Collapse",
      "workstream": "backend",
      "changeType": "added",
      "title": "services/showroom/collapse-branches.ts",
      "detail": "Per branch: create the location row FIRST, then remap children (reusing 0046's SIMPLE_MOVE/DEDUP_MOVE), then soft-delete the store. Sequential + compensating delete (D1 has no transactions). Re-verify group_key before starting; abort STALE if membership moved. Skip and report a branch with no usable address. Chunk every multi-id write at 20.",
      "dependsOn": [
        "P1-schema"
      ],
      "status": "pending"
    },
    {
      "taskKey": "P4-mcp-tools",
      "phase": "P4 — MCP",
      "workstream": "mcp",
      "changeType": "added",
      "title": "list / get / resolve / apply_merge_candidate",
      "detail": "One file per tool. READ_ONLY, READ_ONLY, WRITE, DESTRUCTIVE. apply only on an APPROVED candidate. Gives chat parity with the UI, per the ambiguous-parent doctrine (confirm in either surface, both through the same confirm step).",
      "dependsOn": [
        "P2-scan",
        "P3-collapse-service"
      ],
      "status": "pending"
    },
    {
      "taskKey": "P5-review-ui",
      "phase": "P5 — Review UI",
      "workstream": "frontend",
      "changeType": "added",
      "title": "/admin/shopping/showrooms/merge-review",
      "detail": "Thin Astro shell (class not className, container mx-auto px-4 py-8 pb-12, header block with a 24px lucide icon) + one React island. Per group: switchable keeper, each member with address/phone/site + matched evidence, per-member keep/exclude, approve/reject.",
      "dependsOn": [
        "P2-scan"
      ],
      "status": "pending"
    },
    {
      "taskKey": "P6-qc",
      "phase": "P6 — QC + deploy",
      "workstream": "qc",
      "changeType": "added",
      "title": "scripts/qc/pr_<n>.mjs — preview + prod",
      "detail": "Collapse exercised on a throwaway store pair the test creates and removes — never live rows. Asserts address carried across, children remapped, EXCLUDED member untouched, re-scan yields no new candidate, and 0046's no-runaway-component guard still passes.",
      "dependsOn": [
        "P4-mcp-tools",
        "P5-review-ui"
      ],
      "status": "pending"
    }
  ],
  "successCriteria": [
    "Every 0046 branchCandidate appears as a reviewable candidate row",
    "Approving collapses N stores into 1 business + N locations with no address lost",
    "A member marked EXCLUDED is left completely untouched",
    "Re-running the scan after an apply produces no new candidate for that group",
    "dedup_showroom_stores still reports 0 runaway components",
    "QC green on preview and prod"
  ]
}
