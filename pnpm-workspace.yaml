packages:
  - "."

allowBuilds:
  '@google/genai': true
  core-js-pure: true
  esbuild: true
  msw: true
  protobufjs: true
  sharp: true
  workerd: true

# Pin these transitive versions to match our direct deps (see package.json).
# Keep in sync with the `@cloudflare/workers-types` / `hono` versions there.
overrides:
  '@cloudflare/workers-types': ^4.20260503.1
  hono: ^4.12.16
